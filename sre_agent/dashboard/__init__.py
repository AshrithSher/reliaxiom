"""Read-only demo dashboard (presentation layer).

A live web view projected over the agent's persisted SQLite stores — it never touches the
detection/diagnosis/action core. See DECISIONS.md D-025.
"""
